name: nekocalc
description: A personal calculation toolbox built with Flutter.
publish_to: "none"
version: 1.2.0-beta.1+8

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  characters: ^1.4.1
  cupertino_icons: ^1.0.8
  intl: ^0.19.0
  path: ^1.9.0
  sqflite: ^2.3.3+1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
